<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baas.securities.repository.dao.AccountDao">
    <select id="findById" resultType="Account">
        select * from `accounts` where id = #{id};
    </select>

    <select id="save" resultType="Account">
        insert into `accounts` (id, user_id, account_number, account_password, balance, is_active, account_type, created_at, last_update)
        values (#{id}, #{userId}, #{account_number}, #{balance}, #{isActive}, #{accountType}, #{createdAt}, #{lastUpdate});
    </select>
</mapper>