<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.baas.bank.user.mapper.UserMapper">

    <insert id="insertUser" parameterType="UserDto">
        INSERT INTO user (login_id, login_password, name, birth_date, phone, email)
        VALUES (#{loginId}, #{loginPassword}, #{name}, #{birthDate}, #{phone}, #{email})
    </insert>
    <insert id="saveSocial" parameterType="UserDto">
        INSERT INTO user (name, phone, email, oauth_provider, oauth_id)
        VALUES (#{name}, #{phone}, #{email}, #{oauthProvider}, #{oauthId})
    </insert>

    <select id="findByLoginId" parameterType="String" resultType="UserDto">
        SELECT * FROM user WHERE login_id = #{loginId}
    </select>

        <select id="findByLoginIdWithoutPassword" parameterType="String" resultType="UserDto">
            SELECT id, login_id, name, birth_date, phone, email, created_at FROM user WHERE login_id = #{loginId}
        </select>

        <select id="findByEmail" parameterType="String" resultType="UserDto">
            SELECT id, login_id, name, birth_date, phone, email, created_at FROM user WHERE email = #{email}
        </select>

        <select id="findById" parameterType="Long" resultType="UserDto">
            SELECT id, login_id, name, birth_date, phone, email, created_at FROM user WHERE id = #{userId}
        </select>
    <select id="findByProviderId" resultType="com.baas.bank.user.dto.UserDto">
        SELECT id, login_id, name, birth_date, phone, email, created_at
        FROM user
        WHERE oauth_provider = #{provider} AND oauth_id = #{oauthId}
    </select>
    <select id="findIdByEmail" resultType="java.lang.Long">
        SELECT id
        FROM user
        WHERE email = #{email}
    </select>

    </mapper>
